apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: provider-kubernetes
  annotations:
    paas-templates-crossplane-provider-source-url: "https://github.com/crossplane-contrib/provider-kubernetes/releases/tag/v0.1.0"
spec:
#  package: "crossplane/provider-kubernetes:main"
  package: "crossplane/provider-kubernetes:v0.3.0"
  controllerConfigRef:
    # Configure http proxy env vars and verbose debug arg for this provider
    name: crossplane-controller-config
