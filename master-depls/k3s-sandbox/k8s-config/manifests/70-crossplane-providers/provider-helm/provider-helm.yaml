#See https://github.com/crossplane-contrib/provider-helm#install
apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: provider-helm
  annotations:
    paas-templates-crossplane-provider-source-url: "https://github.com/crossplane-contrib/provider-helm/releases/tag/v0.9.0"
spec:
  package: "crossplane/provider-helm:v0.10.0"
  controllerConfigRef:
    # Configure http proxy env vars and verbose debug arg for this provider
    name: crossplane-controller-config
